server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: file:///E:/vetcare/config-repo
          default-label: main

management:
  endpoints:
    web:
      exposure:
        include: "*"